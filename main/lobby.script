function init(self)
    msg.post(".", "acquire_input_focus")
    msg.post("@render:", "use_fixed_fit_projection", { near = -1, far = 1 })
    print("Lobby scene ready")
end

function update(self, dt)
end

function on_input(self, action_id, action)
end
